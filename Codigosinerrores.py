lista = [2, 6, 9, 5]
total = 0
for i in range(len(lista)):
    total += lista[i]
    print(total)
#errores corregidos
#libreria tkinter importada pero no usada
#linea en blanco
#no se utilizan espacios alrededor del operador en la definición de "total"
#espacio en blanco antes del parentesis del print
